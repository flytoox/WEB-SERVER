

# //*  Just remember that, for chunked request, your server needs to unchunk
# it,

# //* The output of the CGI. If no content_length is returned
# from the CGI, EOF will mark the end of the returned data.

# //*


#Sample of the required directives
# server {


#     listen localhost:8080;

#     server_names VALUE;

#     error_pages  VALUE;

#     client_max_body_size VALUE;

#     root /var/www/html; 

#     allowed-http-methods VALUE; //GET & POST

#     return 301 http://localhost ; //HTTP redirection PORT HOST

#     autoindex VALUE; //directory listing

#     error_page 404 /another/whoops.html;

#     index VALUE; // default file to answer if the request is a directory

#     #location ~ \.php$ {
#     #    root           html;
#     #    fastcgi_pass   127.0.0.1:9000;
#     #    fastcgi_index  index.php;
#     #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
#     #    include        fastcgi_params;
#     #} // CGI 

# }

server {

    listen 3333;
    server_name  localhost;
    autoindex on;

    root /Users/sizgunan;

    location / {
        root /var/www
    }

    location /another {
        root /var/www
        autoindex
        reti
    }
}


server {

    listen 8080;
    server_name  localhost;
    autoindex on;

    root /Users/sizgunan;


    location /another {
        root /var/www
    }
}

server {

    listen 8000;
    server_name  localhost;
    autoindex on;

    root /Users/sizgunan;


    location /another {
        root /var/www
    }
}

server {
    listen 4121 3239;
    server_name  localhost;
    autoindex on;

    root /Users/sizgunan/Music;

}


# server {

#     listen 80 default_server;

#     listen [::]:80 default_server; 

#     root /var/www/html;  

#     index index.html; 

#     server_name _;  

#     location / {
#        try_files $uri $uri/ =404;
#     }

#     location /api/ {
#            proxy_pass http://localhost:8080/;
#            proxy_http_version 1.1;
#            proxy_set_header Upgrade $http_upgrade;
#            proxy_set_header Connection 'upgrade';
#            proxy_set_header Host $host;
#            proxy_cache_bypass $http_upgrade;
#     }

#     location /images {
#            alias uploads/images/;
#     }
# }
